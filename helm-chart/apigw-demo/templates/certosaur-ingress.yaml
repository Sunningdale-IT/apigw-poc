{{- if and .Values.certosaur.enabled .Values.certosaur.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "apigw-demo.fullname" . }}-certosaur
  labels:
    {{- include "apigw-demo.certosaur.labels" . | nindent 4 }}
  {{- with .Values.certosaur.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.certosaur.ingress.className }}
  {{- if .Values.certosaur.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.certosaur.ingress.host }}
      secretName: {{ .Values.certosaur.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.certosaur.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "apigw-demo.fullname" . }}-certosaur
                port:
                  number: {{ .Values.certosaur.service.port }}
{{- end }}
